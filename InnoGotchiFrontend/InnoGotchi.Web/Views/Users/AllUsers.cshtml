@{
    ViewData["Title"] = "All users";
}
@model IEnumerable<UserViewModel>;
<div class="text-center">
    <h1 class="display-4">Users</h1>
    <table class="table">
        <tr>
            <td></td>
            <td></td>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td align="left">
                    <img src="data:image/png;base64,@(Convert.ToBase64String(item.User.Avatar))" width="60" height="60" />
                    @item.User.FirstName @item.User.LastName
                </td>
                <td valign="middle">
                    @switch (item.RequestState)
                    {
                        case RequestState.Confirmed :
                            <a class="btn btn-success" aria-disabled="true">Your friend:)</a>
                            break;
                        case RequestState.Sent :
                            <a class="btn btn-danger">Cancel</a>
                            break;
                        case RequestState.Received:
                            <a class="btn btn-success">Accept</a>
                            <a class="btn btn-danger">Cancel</a>
                            break;
                        default:
                            <a class="btn btn-sm btn-primary" asp-action="Create" asp-route-receiverId="@item.User.Id" asp-controller="Requests">Send friendship request</a>
                            break;
                    }
                </td>
            </tr>
        }
    </table>
</div>
